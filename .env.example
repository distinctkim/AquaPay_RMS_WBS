# Africa's Talking SMS credentials (used by both the main app and rms/scheduler.py)
AT_API_KEY=atsk_14ee8407bc260e33c5bb098feaaf434d8feb4f0a08f36a901286c90119994a0652b6fbee
AT_USERNAME=distinctkim
AT_SANDBOX=false

# Database (SQLite by default; switch to a postgresql:// URL for production)
RMS_DATABASE_URL=sqlite:///rms_data.db

# Reminder timing
RMS_REMINDER_DAYS_BEFORE_DUE=7

# Optional: extra CA bundle for corporate proxy / TLS-inspection environments
# EXTRA_CA_BUNDLE=/path/to/extra_ca.pem
